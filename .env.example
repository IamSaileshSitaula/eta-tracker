# ============================================================================
# ETA Tracker - Environment Configuration
# Copy this file to .env and fill in your values
# ============================================================================

# ----------------------------------------------------------------------------
# Backend Server
# ----------------------------------------------------------------------------
SECRET_KEY=dev-secret-key-change-in-production-use-random-string
FLASK_ENV=development

# ----------------------------------------------------------------------------
# Database (PostgreSQL)
# ----------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_password_here

# Alternative: Full database URL (overrides individual settings)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# ----------------------------------------------------------------------------
# Weather API (Optional but Recommended)
# ----------------------------------------------------------------------------
# OpenWeatherMap - Free tier: 60 calls/min, 1M calls/month
# Sign up: https://openweathermap.org/api
# Without key: System uses mock weather data
OPENWEATHER_API_KEY=

# ----------------------------------------------------------------------------
# Traffic APIs (Optional - Choose One)
# ----------------------------------------------------------------------------
# Google Maps Traffic API
# https://developers.google.com/maps/documentation/directions/get-api-key
GOOGLE_MAPS_API_KEY=

# HERE Traffic API
# https://developer.here.com/
HERE_API_KEY=

# TomTom Traffic API
# https://developer.tomtom.com/
TOMTOM_API_KEY=

# Note: Without traffic API, system uses time-based mock traffic data

# ----------------------------------------------------------------------------
# Routing Configuration
# ----------------------------------------------------------------------------
# Valhalla routing server (optional)
# Leave empty to use OSRM public server
VALHALLA_URL=

# OSRM routing server (fallback)
# Default uses public server, or set up your own
OSRM_URL=https://router.project-osrm.org

# ----------------------------------------------------------------------------
# Update Intervals (seconds)
# ----------------------------------------------------------------------------
ETA_UPDATE_INTERVAL=30
GPS_PING_INTERVAL=15

# ----------------------------------------------------------------------------
# GTFS Transit Mode (Optional)
# ----------------------------------------------------------------------------
# For public transit tracking mode
# GTFS_STATIC_ZIP_URL=https://example.com/gtfs.zip
# GTFS_VEHICLE_POSITIONS_URL=https://example.com/vehicle-positions
# GTFS_TRIP_UPDATES_URL=https://example.com/trip-updates
